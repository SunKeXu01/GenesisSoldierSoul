# 正式武器复验报告（2026-08-04）

## 结论

`M9 / Pistol01` 回退、`Shotgun01`、`Knife01`、`Grenade01` 以及任意两类武器连续交叉切换均通过本轮正式验收。最终 WebGL 构建成功，产物大小为 `299,868,088` 字节。

## 本轮修复

- `Shotgun01` 从单射线固定伤害改为服务端权威的 8 弹丸散布：中心 1 发、内圈 4 发、外圈 3 发；每弹丸 10 点伤害，并按目标聚合命中与死亡消息。训练场使用同一组确定性散布参数。
- `Grenade01` 的归档模型根节点边界需要运行时归一化。此前应用通用第一人称构图时覆盖了该偏移，导致手雷落到视口外；现将归一化偏移叠加到正式构图，切换与投掷前均保持可见。
- 增补霰弹枪散布、近战权威距离/伤害和手雷视口可见性回归测试。

## 自动化结果

- 服务端 Vitest：`22/22` 通过。覆盖霰弹枪 8 弹丸伤害与射速门禁、近战 `2.2m / 50` 伤害、手雷单生命数量、延时半径伤害与复活补充。
- Unity EditMode：`16/16` 通过，报告为 `recovery/formal-weapons-editmode.xml`。
- Unity PlayMode：`4/4` 通过，报告为 `recovery/formal-weapons-playmode.xml`；包含手雷归一化应用构图后仍处于武器相机视口内的断言。
- WebGL 构建：成功，日志为 `recovery/webgl-formal-weapons-grenade-fix-build.log`。

## WebGL 浏览器验收

- `Shotgun01`：手持模型和 HUD 正确显示 `8/32`；开火后减为 `7/32`；泵动/射速门禁期间的第二次输入未重复扣弹；换弹显示 `RELOADING`，完成后由 `6/32` 变为 `8/30`。
- `Knife01`：持刀、手臂重组、挥刀位移及动作后恢复 `READY` 均正常；命中范围与伤害由服务端测试锁定。
- `Grenade01`：最终构建中手雷、手部与拉环可见；投掷后生成飞行物，爆炸效果、半径伤害和自动回切近战正常。
- 连续交叉切换：`1 → 2 → 3` 稳定结束于 Knife，`2 → 1` 稳定结束于 Shotgun01；HUD、模型与弹药一致，无叠模、串动作或串弹药。
- 控制台没有武器逻辑错误；仅保留金字塔地图资源原有的负缩放 `BoxCollider` 警告。

## 音频资源

`Shotgun01` 继续绑定归档专属路径 `OriginalGame/Audio/Shotgun01/{fire,reload,deploy}`；运行时初始化日志识别为 `Primary=SHOTGUN 01`，三类事件分别解析为 `fire / reload / deploy`。浏览器自动化无法对听感作主观判定，因此这里只确认资源绑定与动作触发链路。
