# 参考画面风格 HUD 恢复（2026-08-03）

## 恢复范围

- 保留原游戏圆形雷达、方向指针和顶部“雷霆战警 / 烈火联盟”比分资源。
- 准星统一使用恢复后的透明十字纹理；普通状态为浅绿，命中时短暂变红。
- 左下角新增参考画面配色的生命/护甲双仪表、数值和独立进度条。
- 右下角新增武器图标、武器名、当前弹匣与备用弹药；换弹时显示 `RELOADING`。
- 弹药面板上方新增四槽武器栏，按 `1-4` 切换时对应槽位高亮，并同步切换武器图标和弹药信息。
- 顶部原比分框下方新增动态击杀/死亡/回合时间条。
- 雷达下方新增地图名和在线人数条。
- `Tab` 计分板新增深色半透明底板、玩家名、击杀、死亡和存活状态；保留长按显示，并为自动验收增加不足一秒的按键驻留。
- 右上角击杀信息使用最多四行的紧凑半透明信息条，真实网络击杀事件会立即刷新并在 4.5 秒后隐藏。
- 护甲系统尚无拾取或服务端伤害吸收规则，因此当前 HUD 如实显示 `0`，没有伪造不可用的护甲值。

## WebGL 实机验收

- New Construction Site 已确认雷达、地图/在线人数、准星、生命/护甲、弹药、武器栏和顶部信息均没有超出画布。
- M4A1 与 Pistol01 切换后，武器栏高亮、图标、名称和弹药数字同步更新。
- 独立 QA 角色造成一次手枪伤害后，生命从 `100` 更新为 `66`，生命条按相同比例缩短；护甲继续保持真实的 `0`。
- 三人在线房间完成真实 QA 角色击杀，右上角正确显示 `QA-ACTOR > QA-ACTOR`，并在超时后自动消失。
- `Tab` 计分板在 WebGL 中显示正常，单人和多人 roster 均由服务器状态填充。
- 最终完整构建成功，7 张地图、方向动画 9/9、跳跃、战斗骨骼 3/3 和第三人称武器资源 4/4 均通过。

## 构建证据

- 第一轮：`recovery/webgl-reference-hud-build.log`
- 布局修正：`recovery/webgl-reference-hud-final-build.log`
- 最终精修：`recovery/webgl-reference-hud-polish-build.log`

## 后续

- 地图逐张修复阶段需要分别检查浅色天空、暗场和高饱和材质背景下的 HUD 对比度。
- 若后续恢复护甲拾取与伤害吸收规则，应把当前 `armor` 值接入服务端快照；现阶段不改变既有战斗平衡。
